const bot_values = require("../bot_data").bot_values;

module.exports = {

    help_msg: "Permet d'ajouter son anniversaire.\n" +
    `${bot_values['bot_prefix']}anniversaire <pseudo> <jour>/<mois>`

};
